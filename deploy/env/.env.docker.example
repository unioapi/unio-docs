# 构建镜像的本机参数；实际值放同目录的 .env.docker（gitignore），每台要构建镜像的机器一份：
#   cp deploy/env/.env.docker.example deploy/env/.env.docker
# 只有 deploy/build-image.sh 读这个文件。

# 烤进产物的环境：dev | test | prod，对应仓库根目录的 .env.dev / .env.test / .env.prod（NEXT_PUBLIC_* 在构建期固化）。
# 本机开发写 dev，测试服务器写 test，生产服务器写 prod；镜像 tag 不带环境后缀，每台机器只构建、只运行自己环境的产物。
BUILD_ENV=dev

# 装依赖用 bun（与 package.json 的 packageManager 一致），构建与运行用 node。
BUN_IMAGE=oven/bun:1.3.12-alpine
NODE_IMAGE=node:24-alpine
